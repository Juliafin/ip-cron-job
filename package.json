{
  "name": "ip-cron-job",
  "version": "1.0.0",
  "description": "This application retrieves the user's ip address from an external api and sends an email notifying them that it has been updated",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ip", "cron", "job"],
  "author": "Julia Finarovsky",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv": "^8.1.0",
    "node-cron": "^2.0.3",
    "nodemailer": "^6.3.1"
  }
}
